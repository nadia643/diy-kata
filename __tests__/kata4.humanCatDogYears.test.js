const { humanCatDogYears } = require('../src');

describe('humanCatDogYears', () => {
    it('returns human years', () => {
        expect(humanCatDogYears()).toBe()
    });
    it('returns cat years', () => {
        expect(humanCatDogYears()).toBe()
    });
    it('returns dog years', () => {
        expect(humanCatDogYears()).toBe()
    });

});
// Look Ma, no handlebars!!!
